# AI Guardian - Workspace Rules

- **No Automated / Direct Deployments**: Do not execute any deployment commands, build uploads, or run scripts that publish the code to production. All deployments must be done manually by the user after checking the project state.
